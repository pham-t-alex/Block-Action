using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WorldSelectInitialize : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        AudioController.audioController.PlayBGM("titletheme");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
